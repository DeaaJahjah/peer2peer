enum AuthState { waiting, notSet }

enum SearchType { all, houses, kids, furniture, sport, elecotronic }

enum DataState {
  loading,
  done,
  notSet,
  failure,
  empty,
  hasData,
  offline,
}

enum ServiceType { need_to, provided_to }
